1) make sure you have docker and docker compose installed :)
2) navigate on a terminal to location where you downloaded this folder
3) if in linux> sudo docker-compose up --build  if in windows> docker-compose up --build
4) Wait, wait wait till its done configuring and installing dependencys
5)once its completed the install and no error were found you are good to start loading information to the db
6) docker-compose ps < this is a comand to check if containers are alive and port specifications.
